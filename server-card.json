{
  "serverInfo": {
    "name": "shell-session-mcp",
    "version": "1.2.40"
  },
  "tools": [
    {
      "name": "shell_session",
      "description": "Manage interactive or persistent shell sessions. First call action=\"help\" when unsure.",
      "inputSchema": {
        "type": "object",
        "properties": {
          "action": {
            "type": "string",
            "description": "Action name. Call action=\"help\" first when unsure."
          },
          "args": {
            "type": "object",
            "additionalProperties": {},
            "default": {},
            "description": "Action arguments. Use action=\"help\" for details."
          }
        },
        "required": [
          "action"
        ],
        "additionalProperties": false,
        "$schema": "http://json-schema.org/draft-07/schema#"
      }
    }
  ],
  "resources": [],
  "prompts": []
}
